
stylecow <input:output> [options]

stylecow plugin -list
stylecow plugin -install <plugin>
stylecow plugin -uninstall <plugin>

stylecow default:plugins -list
stylecow default:plugins -add
stylecow default:plugins -remove

stylecow default:support -list
stylecow default:support -explorer
stylecow default:support -firefox
stylecow default:support -chrome
stylecow default:support -ios
stylecow default:support -android
stylecow default:support -opera
stylecow default:support -safari

stylecow default:code -list
stylecow default:code -set
